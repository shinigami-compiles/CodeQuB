your_project_root/
│
├── app.py                          # Flask app (all routes: landing, input, result, breakdown, about, APIs)
├── generate_dataset.py             # Synthetic dataset generator (15k balanced rows)
├── train_model.py                  # Multi-task ANN trainer (saves model, scaler, metadata)
├── explainability.py               # Feature contribution computation (Grad × Input)   [already discussed]
├── recommendations.py              # Rule-based recommendations for quality & risk   [already discussed]
├── report_generator.py             # Builds HTML report for a single analysis        [already discussed]
│
├── requirements.txt                # Python dependencies (TF, Flask, numpy, etc.)
├── README.md                       # Full project documentation & usage guide
│
├── data/
│   └── code_quality_bug_risk_dataset_15k_balanced.csv
│                                   # Synthetic dataset generated by generate_dataset.py
│
├── models/
│   ├── multitask_ann_model/        # Saved Keras model (from train_model.py)
│   ├── feature_scaler.pkl          # StandardScaler for 22 input features
│   └── metadata.json               # Feature names, descriptions, stats, label mapping, etc.
│
├── templates/
│   ├── base.html                   # Base layout: header, nav (Home/About), footer, includes CSS/JS
│   ├── landing.html                # Landing page: system name, description, "Start Code Analysis" button
│   ├── input.html                  # Page with:
│   │                               #   - Manual metrics form (22 features)
│   │                               #   - Single-row CSV upload section
│   ├── result.html                 # Shows predictions: quality score, bug risk class, probabilities, top factors
│   ├── breakdown.html              # Detailed breakdown: full feature impacts, recommendations, metrics table
│   └── about.html                  # Explains what AI does, pipeline, CSV structure, feature list, template CSV download
│
└── static/
    ├── css/
    │   └── style.css               # Dark black + neon cyan cyberpunk theme for all pages
    │
    ├── js/
    │   └── main.js                 # Frontend logic:
    │                               #   - Toggle manual/CSV modes
    │                               #   - Call /api/report on "Download Full HTML Report"
    │
    └── files/
        └── template_metrics.csv    # Single-row CSV template for users to download & fill
